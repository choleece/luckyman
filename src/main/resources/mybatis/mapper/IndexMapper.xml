<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sf.luckyman.mapper.IndexMapper" >
    <insert id="insertSigner">
        insert into signer(name, gender, email, tel, school, major, graduation)
        values(#{name}, #{gender}, #{email}, #{tel}, #{school}, #{major}, #{graduation})
    </insert>

    <select id="listSigner" resultType="com.sf.luckyman.entity.Signer">
        select name, tel from signer
    </select>
</mapper>